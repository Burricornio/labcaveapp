# LabCave 2018 Movielist app

Download repository:

```https://github.com/Burricornio/lavcaveapp.git```

Install all development dependencies using:

```$ npm install o $ yarn install```

Run the development server with:

```$ npm run dev 0 $ yarn dev```